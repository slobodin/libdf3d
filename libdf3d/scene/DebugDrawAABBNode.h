#pragma once

#include "DebugDrawNode.h"

namespace df3d { namespace scene {

    /*
class AABB;

class DebugDrawAABBNode : public DebugDrawNode
{
public:
    DebugDrawAABBNode();
    ~DebugDrawAABBNode();

    void recreate(const AABB &aabb);
    void setTransformation(const glm::mat4 &tr);

    shared_ptr<Node> clone() const;
};
*/
} }