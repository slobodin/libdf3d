#pragma once

#include <render/RenderOperation.h>

FWD_MODULE_CLASS(render, RenderPass)

namespace df3d { namespace scene {

/*class DebugDrawNode : public Node
{
protected:
    // Assume that debug draw nodes consist only of one render operation.
    // Subclasses of this class should init it properly.
    render::RenderOperation m_ro;

public:
    DebugDrawNode();
    ~DebugDrawNode();

    render::RenderOperation getRenderOperation();
};*/

} }